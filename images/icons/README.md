# 图标文件说明

此目录用于存放导航按钮图标文件。

## 需要的图标文件

请在此目录下添加以下图标文件（建议尺寸：40px × 40px，PNG格式，支持透明背景）：

- `navigate.png` - 导航按钮图标
- `phone.png` - 电话按钮图标

## 图标要求

- 格式：PNG（支持透明背景）
- 尺寸：建议 40px × 40px（小程序会自动缩放）
- 颜色：根据按钮背景色选择合适的图标颜色

## 临时方案

如果没有图标文件，代码会回退使用emoji图标显示。
